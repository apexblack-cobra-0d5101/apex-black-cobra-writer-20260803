name: G06 Foreign Pull Request Merge Probe
on:
  workflow_dispatch:
  push:
    branches: [main]
permissions:
  contents: write
  pull-requests: write
jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - name: Attempt A public PR merge from B
        uses: actions/github-script@v7
        env:
          TARGET_OWNER: apexblackrhino1782764375
          TARGET_REPO: apex-black-rhino-public-writer-20260803
          TARGET_PR: "3"
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            async function request(method, route, params = {}) {
              try {
                const response = await github.request(method + " " + route, {
                  ...params,
                  headers: { accept: "application/vnd.github+json" }
                });
                return { status: response.status, data: response.data };
              } catch (error) {
                return { status: error.status || 0, data: error.response?.data || String(error.message) };
              }
            }
            const route = "/repos/" + process.env.TARGET_OWNER + "/" + process.env.TARGET_REPO;
            const before = await request("GET", route + "/pulls/" + process.env.TARGET_PR);
            const merge = await request("PUT", route + "/pulls/" + process.env.TARGET_PR + "/merge", {
              commit_title: "G06 foreign fork merge canary",
